const { expect } = require("chai");

const createProgramTypeIDTable = require("./create-program-type-id-table");

describe("createProgramTypeIDTable", () => {

  it("should throw if it does not recieve a array of objects of the expected shape as the single parameter to the function", () => {

  });

  it("should return an object mapping the program type ids to the main progrma names in the the program type id config", () => {

  });

});

