Write a new data access layer to work with updated config system

* replace shared/util/data-access with new implementation that
  dynamically loads and parses json files from dist/data/ on request
* add new dependency: fetch polyfill
* add new dependency: promise polyfill

