Fix bug in store initialization

* buglog commit: b7534eb             
* Remove duplicate store initialization code from app bootstrapping
  code in src/index.tsx; replace with reference to redux store
  initialized in src/shared/redux/store.
