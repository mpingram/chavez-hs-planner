Restructure config system 

* DONE move all data out of app bundle
  ** DONE move all files out of src/shared/data
  ** DONE remove data-related constants out of
    src/shared/constants
  ** DONE move requirement function code to
    config/ folder

* WIP reorganize config/ folder

* TODO move all python scripts in config/build to
  build-scripts/.
  ** CONSIDER rewriting python scripts in node js

* TODO rewrite build scripts to take 
  configuration files as input and produce the
  same files previously located in src/shared/data
  as output, saving those files in dist/data.

* TODO rewrite app data access scripts to load data
  via ajax request from ./data/
  

