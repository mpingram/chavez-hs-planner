Remove dead code

* remove shared/util/clone
* remove shared/types/additional-requirements
* remove icon types in shared/types/
* remove circled up/down arrows in shared/components/icons
* combine ScoreType enum and ScoreType type
* remove empty folder shared/images/
