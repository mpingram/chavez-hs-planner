BUGFIX clicking on hsProgramElement closes hsGroup

* It was event bubbling kind of, but mostly another error on
  my part -- I accidentally put the onClick=close hsGroup event
  handler on the root HSGroup element, instead of the hsGroup.header
  element.
