Rewrite build scripts in js

* rewrite python build scripts in javascript
* split up each build script function into separate module
* refactor each build script to accept objects as parameters,
  rather than filenames.

