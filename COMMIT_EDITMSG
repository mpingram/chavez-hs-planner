Doodle around with a debouncing higher-order-component

* NOTE it is garbage, but very useful.
* NOTE but it's a good thing to explore. I think the implementation is
  awful but the idea is good -- should refactor and test the stuffing
  out of it before it goes into main, though.
